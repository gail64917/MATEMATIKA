function main
    FULL_MATRIX = [ 6 10 4 5 8 ;
        8 10 7 9 11;
        4 8 9 10 6;
        5 9 6 11 10;
        6 11 6 3 9];
    Maximize = 1;
    Reduced_Matrix = Step1(FULL_MATRIX, Maximize);
    [result, choosen] = Step2(Reduced_Matrix);
%     while (result == 0)
%         Reduced_Matrix = Step3(Reduced_Matrix);
%         [result, choosen] = Step2(Reduced_Matrix);
%         disp(choosen);
%     end
    disp(choosen);
    disp(result
end